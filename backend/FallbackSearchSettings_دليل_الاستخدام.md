# دليل استخدام إعدادات Fallback Search

## 📋 نظرة عامة

هذا الدليل يشرح كيفية استخدام إعدادات **استراتيجية البحث الذكية مع التخفيف التدريجي** بشكل عملي.

---

## 🎯 السيناريوهات الشائعة

### 1️⃣ الإعداد الافتراضي (موصى به للإنتاج)

```json
{
  "EnableFallback": true,
  "MinResultsThreshold": 5,
  "EnableMinorRelaxation": true,
  "EnableModerateRelaxation": true,
  "EnableMajorRelaxation": true,
  "EnableAlternativeSuggestions": true,
  "PriceRelaxationMinor": 0.15,
  "PriceRelaxationModerate": 0.30,
  "PriceRelaxationMajor": 0.50,
  "ShowRelaxationInfo": true,
  "LogRelaxationSteps": true
}
```

**متى تستخدمه؟**
- الاستخدام اليومي في الإنتاج
- توازن بين دقة النتائج وتجربة المستخدم
- يضمن عدم ظهور "لا توجد نتائج" إلا في حالات نادرة جداً

---

### 2️⃣ إعدادات متحفظة (تخفيف أقل)

```json
{
  "EnableFallback": true,
  "MinResultsThreshold": 10,
  "EnableMinorRelaxation": true,
  "EnableModerateRelaxation": true,
  "EnableMajorRelaxation": false,
  "EnableAlternativeSuggestions": false,
  "PriceRelaxationMinor": 0.10,
  "PriceRelaxationModerate": 0.20,
  "ShowRelaxationInfo": true
}
```

**متى تستخدمه؟**
- عندما تريد نتائج أكثر دقة وأقل تخفيفاً
- عندما يكون لديك قاعدة بيانات كبيرة بخيارات كثيرة
- للأسواق التنافسية حيث الدقة مهمة

**التأثير:**
- توسيع أقل في السعر (10% ثم 20%)
- لا يصل للتخفيف الكبير أو البدائل
- يحتاج 10 نتائج على الأقل قبل التوقف

---

### 3️⃣ إعدادات مرنة جداً (تخفيف أكبر)

```json
{
  "EnableFallback": true,
  "MinResultsThreshold": 3,
  "EnableMinorRelaxation": true,
  "EnableModerateRelaxation": true,
  "EnableMajorRelaxation": true,
  "EnableAlternativeSuggestions": true,
  "PriceRelaxationMinor": 0.20,
  "PriceRelaxationModerate": 0.40,
  "PriceRelaxationMajor": 0.60,
  "RadiusMultiplierMinor": 2.0,
  "RadiusMultiplierModerate": 3.0,
  "RadiusMultiplierMajor": 5.0,
  "ShowRelaxationInfo": true
}
```

**متى تستخدمه؟**
- عندما تكون قاعدة البيانات صغيرة أو خيارات محدودة
- في المناطق النائية أو المدن الصغيرة
- عندما تريد ضمان حصول المستخدم على نتائج دائماً

**التأثير:**
- توسيع كبير في السعر (20% → 40% → 60%)
- توسيع جغرافي أكبر بكثير
- يتوقف عند 3 نتائج فقط

---

### 4️⃣ إعدادات التطوير والاختبار

```json
{
  "EnableFallback": true,
  "MinResultsThreshold": 1,
  "EnableMinorRelaxation": true,
  "EnableModerateRelaxation": true,
  "EnableMajorRelaxation": true,
  "EnableAlternativeSuggestions": true,
  "ShowRelaxationInfo": true,
  "LogRelaxationSteps": true,
  "LoggingDetailLevel": 2
}
```

**متى تستخدمه؟**
- أثناء التطوير والتجربة
- لفهم كيفية عمل كل مرحلة
- لمراقبة السلوك بدقة

**التأثير:**
- يتوقف عند أول نتيجة (MinResultsThreshold = 1)
- تسجيل تفصيلي جداً في Logs

---

### 5️⃣ تعطيل الميزة كاملاً (البحث الكلاسيكي)

```json
{
  "EnableFallback": false
}
```

**متى تستخدمه؟**
- عندما تريد العودة للبحث العادي
- للمقارنة بين السلوكين
- إذا واجهت مشاكل في الأداء (نادراً)

**التأثير:**
- البحث يعمل كما كان قبل تطبيق الميزة تماماً
- إذا لم توجد نتائج → "لا توجد نتائج"

---

## 🔧 تخصيص الإعدادات حسب الحاجة

### سيناريو: مستخدمون يشتكون من نتائج بعيدة عن طلباتهم

**المشكلة:** التخفيف كبير جداً والنتائج غير دقيقة

**الحل:**
```json
{
  "MinResultsThreshold": 10,
  "PriceRelaxationMinor": 0.10,
  "PriceRelaxationModerate": 0.20,
  "EnableMajorRelaxation": false
}
```

---

### سيناريو: كثير من المستخدمين يحصلون على "لا توجد نتائج"

**المشكلة:** التخفيف غير كافٍ

**الحل:**
```json
{
  "MinResultsThreshold": 3,
  "PriceRelaxationMinor": 0.20,
  "PriceRelaxationModerate": 0.35,
  "PriceRelaxationMajor": 0.60,
  "RadiusMultiplierMinor": 2.0,
  "RadiusMultiplierModerate": 3.0,
  "RadiusMultiplierMajor": 5.0
}
```

---

### سيناريو: أداء بطيء (نادراً)

**المشكلة:** البحث يأخذ وقت طويل

**الحل:**
```json
{
  "MinResultsThreshold": 10,
  "EnableMajorRelaxation": false,
  "EnableAlternativeSuggestions": false,
  "LogRelaxationSteps": false
}
```

---

### سيناريو: لا تريد عرض معلومات التخفيف للمستخدم

**المشكلة:** الرسائل مربكة للمستخدمين

**الحل:**
```json
{
  "ShowRelaxationInfo": false
}
```

**التأثير:**
- الاستراتيجية تعمل لكن بدون رسائل للمستخدم
- النتائج تظهر عادية بدون توضيح التخفيفات

---

## 📊 جدول المقارنة السريع

| الإعداد | قيمة متحفظة | قيمة متوازنة (افتراضي) | قيمة مرنة |
|---------|-------------|----------------------|----------|
| **MinResultsThreshold** | 10 | 5 | 3 |
| **PriceRelaxationMinor** | 0.10 (10%) | 0.15 (15%) | 0.20 (20%) |
| **PriceRelaxationModerate** | 0.20 (20%) | 0.30 (30%) | 0.40 (40%) |
| **PriceRelaxationMajor** | 0.30 (30%) | 0.50 (50%) | 0.60 (60%) |
| **RadiusMultiplierMinor** | 1.2x | 1.5x | 2.0x |
| **RadiusMultiplierModerate** | 1.5x | 2.0x | 3.0x |
| **RadiusMultiplierMajor** | 2.0x | 3.0x | 5.0x |
| **EnableMajorRelaxation** | false | true | true |

---

## 🧪 كيفية الاختبار

### 1. اختبار البحث الدقيق (Exact Match)

```http
GET /api/search/units?city=صنعاء&minPrice=5000&maxPrice=8000&guestsCount=4
```

**المتوقع:** إذا وجد نتائج دقيقة → RelaxationLevel = Exact

---

### 2. اختبار التخفيف البسيط

```http
GET /api/search/units?city=صنعاء&minPrice=7000&maxPrice=7500&requiredAmenities=wifi,parking,breakfast&minRating=4.8
```

**المتوقع:** 
- إذا لم توجد نتائج دقيقة → RelaxationLevel = MinorRelaxation
- UserMessage يوضح التخفيفات المطبقة

---

### 3. اختبار "لا توجد نتائج نهائياً"

```http
GET /api/search/units?city=مدينة_غير_موجودة&minPrice=1&maxPrice=2
```

**المتوقع:**
- RelaxationLevel = AlternativeSuggestions
- SuggestedActions تحتوي على اقتراحات مفيدة

---

## 📝 ملاحظات مهمة

### ✅ أفضل الممارسات

1. **لا تعطل جميع المراحل** - على الأقل اترك Minor و Moderate
2. **MinResultsThreshold بين 3-10** - أقل من 3 قد يوقف مبكراً، أكثر من 10 قد يخفف كثيراً
3. **راقب Logs في البداية** - لفهم سلوك المستخدمين
4. **اختبر قبل الإنتاج** - جرب الإعدادات في بيئة تطوير أولاً

---

### ⚠️ تحذيرات

1. **لا تعطل EnableFallback بدون سبب** - ستفقد الميزة الرئيسية
2. **لا تجعل PriceRelaxation أكثر من 0.80** - قد يكون التخفيف مبالغاً فيه
3. **LoggingDetailLevel = 2 في الإنتاج** - قد يملأ Logs بسرعة
4. **تغيير الإعدادات يتطلب إعادة تشغيل** - ليست Hot Reload

---

## 🎯 الخلاصة

- **الإعداد الافتراضي مناسب لـ 90% من الحالات**
- **عدّل فقط إذا لاحظت مشكلة واضحة**
- **راقب سلوك المستخدمين وعدّل تدريجياً**
- **استخدم Logs لفهم ما يحدث**

---

تم إنشاء هذا الدليل لمساعدتك في فهم واستخدام إعدادات Fallback Search بفعالية.
للأسئلة أو الاستفسارات، راجع الكود المصدري أو الوثائق التقنية.
