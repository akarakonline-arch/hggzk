name: Build iOS & Android Apps

on:
  push:
  pull_request:
  workflow_dispatch:
    inputs:
      app_to_build:
        description: 'Select app to build'
        required: false
        type: choice
        default: 'all'
        options:
          - all
          - hggzk_app
          - hggzkportal_app
          - rezmate_app
          - rezmateportal_app
      platform:
        description: 'Platform to build'
        required: false
        type: choice
        default: 'both'
        options:
          - both
          - ios
          - android
      ssh:
        description: 'Enable SSH debug session (tmate). Only works for workflow_dispatch.'
        type: boolean
        required: false
        default: false

jobs:
  setup:
    name: Setup Build Matrix
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Set up build matrix
        id: set-matrix
        run: |
          if [ "${{ github.event.inputs.app_to_build }}" == "all" ] || [ -z "${{ github.event.inputs.app_to_build }}" ]; then
            echo 'matrix=["hggzk_app","hggzkportal_app","rezmate_app","rezmateportal_app"]' >> $GITHUB_OUTPUT
          else
            echo 'matrix=["${{ github.event.inputs.app_to_build }}"]' >> $GITHUB_OUTPUT
          fi

  build-ios:
    name: Build ${{ matrix.app }} for iOS
    needs: setup
    if: ${{ github.event.inputs.platform != 'android' }}
    runs-on: macos-latest
    timeout-minutes: 60
    env:
      COCOAPODS_DISABLE_STATS: "true"
    strategy:
      fail-fast: false
      matrix:
        app: ${{ fromJson(needs.setup.outputs.matrix) }}
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: 'latest-stable'

      - name: Verify Xcode version
        run: |
          echo "✅ Selected Xcode version:"
          xcodebuild -version
          echo ""
          echo "Developer Directory:"
          xcode-select -p

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Setup App Store Connect API Key (للتوقيع التلقائي)
        env:
          API_KEY_BASE64: ${{ secrets.APPSTORE_API_KEY_BASE64 }}
          API_KEY_ID: ${{ secrets.APPSTORE_API_KEY_ID }}
          API_ISSUER_ID: ${{ secrets.APPSTORE_API_ISSUER_ID }}
        run: |
          set -euo pipefail
          echo "--- Setting up App Store Connect API ---"
          
          # إنشاء المجلد للـ API keys
          mkdir -p ~/private_keys
          
          # فك تشفير الـ API key من base64
          if [ -n "${API_KEY_BASE64:-}" ]; then
            echo "$API_KEY_BASE64" | base64 -d > ~/private_keys/AuthKey_${API_KEY_ID}.p8
            chmod 600 ~/private_keys/AuthKey_${API_KEY_ID}.p8
            echo "✅ App Store Connect API Key configured"
            echo "Key ID: $API_KEY_ID"
            echo "Issuer ID: $API_ISSUER_ID"
            
            # حفظ المعلومات لاستخدامها لاحقاً
            echo "API_KEY_PATH=~/private_keys/AuthKey_${API_KEY_ID}.p8" >> "$GITHUB_ENV"
          else
            echo "⚠️  App Store Connect API Key not provided"
            echo "⚠️  Will fallback to manual signing"
            echo "API_KEY_PATH=" >> "$GITHUB_ENV"
          fi

      - name: SSH debug (tmate)
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.ssh }}
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: true

      - name: Flutter version
        run: flutter --version

      - name: Install CocoaPods
        run: |
          if ! command -v pod >/dev/null; then
            sudo gem install cocoapods
          fi
          pod --version

      - name: Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: |
            ~/Library/Caches/CocoaPods
            ~/.cocoapods
          key: ${{ runner.os }}-pods-${{ matrix.app }}-${{ hashFiles(format('{0}/ios/Podfile', matrix.app), format('{0}/pubspec.lock', matrix.app)) }}
          restore-keys: |
            ${{ runner.os }}-pods-${{ matrix.app }}-

      - name: Flutter clean
        working-directory: ./${{ matrix.app }}
        run: flutter clean

      - name: Flutter pub get
        working-directory: ./${{ matrix.app }}
        run: flutter pub get

      - name: Pod install
        working-directory: ./${{ matrix.app }}
        run: |
          rm -rf ios/Pods ios/Podfile.lock ios/.symlinks
          cd ios
          pod install --repo-update

      - name: Build iOS (PR - no codesign)
        if: ${{ github.event_name == 'pull_request' }}
        working-directory: ./${{ matrix.app }}
        run: flutter build ios --release --no-codesign

      - name: Setup iOS code signing (keychain + provisioning profile)
        if: ${{ github.event_name != 'pull_request' }}
        env:
          IOS_P12_PASSWORD: ${{ secrets.IOS_P12_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.IOS_KEYCHAIN_PASSWORD }}
        run: |
          set -euo pipefail
          
          # تنظيف كلمات المرور من أي مسافات أو أحرف خفية
          IOS_P12_PASSWORD_CLEAN="$(printf %s "$IOS_P12_PASSWORD" | tr -d '\r\n' | sed 's/^[[:space:]]*//;s/[[:space:]]*$//')"
          KEYCHAIN_PASSWORD_CLEAN="$(printf %s "$KEYCHAIN_PASSWORD" | tr -d '\r\n' | sed 's/^[[:space:]]*//;s/[[:space:]]*$//')"
          
          # التحقق من وجود Secrets
          if [ -z "${IOS_P12_PASSWORD_CLEAN:-}" ]; then
            echo "❌ Missing or empty secret: IOS_P12_PASSWORD"
            exit 1
          fi
          if [ -z "${KEYCHAIN_PASSWORD_CLEAN:-}" ]; then
            echo "❌ Missing or empty secret: IOS_KEYCHAIN_PASSWORD"
            exit 1
          fi
          
          # استخدام الملف من الريبو مباشرة
          CERT_P12="cer_ios/ios_development.p12"
          PROFILE="cer_ios/Provision_Profile_2.mobileprovision"
          
          if [ ! -f "$CERT_P12" ]; then
            echo "❌ Missing certificate file: $CERT_P12"
            exit 1
          fi
          if [ ! -f "$PROFILE" ]; then
            echo "❌ Missing provisioning profile: $PROFILE"
            exit 1
          fi
          
          echo "--- Certificate file info ---"
          ls -lh "$CERT_P12"
          file "$CERT_P12"
          shasum -a 256 "$CERT_P12"
          
          # اختبار كلمة المرور مع OpenSSL أولاً
          echo ""
          echo "--- Testing password with OpenSSL ---"
          if openssl pkcs12 -in "$CERT_P12" -noout -info -passin pass:"$IOS_P12_PASSWORD_CLEAN" 2>&1 | head -10; then
            echo "✅ OpenSSL: Password verified successfully"
          else
            echo "❌ OpenSSL: Password verification failed"
            exit 1
          fi
          
          # إنشاء keychain
          echo ""
          echo "--- Creating keychain ---"
          security create-keychain -p "$KEYCHAIN_PASSWORD_CLEAN" build.keychain
          security set-keychain-settings -lut 21600 build.keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD_CLEAN" build.keychain
          security list-keychains -d user -s build.keychain
          echo "✅ Keychain created and configured"
          
          # الحل الجذري: استيراد الشهادة والمفتاح بشكل منفصل (PEM format)
          # هذا يتجنب مشاكل PKCS#12 مع security على macOS تماماً
          echo ""
          echo "--- Extracting certificate and private key (PEM format) ---"
          
          CERT_PEM="$RUNNER_TEMP/cert.pem"
          KEY_PEM="$RUNNER_TEMP/key.pem"
          CA_CERTS="$RUNNER_TEMP/ca.pem"
          
          # استخراج الشهادة
          openssl pkcs12 -in "$CERT_P12" -clcerts -nokeys -out "$CERT_PEM" \
            -passin pass:"$IOS_P12_PASSWORD_CLEAN"
          
          # استخراج المفتاح الخاص (بدون تشفير باستخدام -nodes)
          openssl pkcs12 -in "$CERT_P12" -nocerts -nodes -out "$KEY_PEM" \
            -passin pass:"$IOS_P12_PASSWORD_CLEAN"
          
          # استخراج CA certificates (إن وُجدت)
          openssl pkcs12 -in "$CERT_P12" -cacerts -nokeys -out "$CA_CERTS" \
            -passin pass:"$IOS_P12_PASSWORD_CLEAN" 2>/dev/null || true
          
          echo "✅ Extracted certificate and key to PEM format"
          ls -lh "$CERT_PEM" "$KEY_PEM"
          
          # استيراد الشهادة للـ keychain
          echo ""
          echo "--- Importing certificate to keychain ---"
          if security import "$CERT_PEM" -k build.keychain -T /usr/bin/codesign -T /usr/bin/security; then
            echo "✅ Certificate imported"
          else
            echo "❌ Certificate import failed"
            exit 1
          fi
          
          # استيراد المفتاح الخاص للـ keychain
          echo "--- Importing private key to keychain ---"
          if security import "$KEY_PEM" -k build.keychain -T /usr/bin/codesign -T /usr/bin/security -A; then
            echo "✅ Private key imported"
          else
            echo "❌ Private key import failed"
            exit 1
          fi
          
          # استيراد CA certificates إن وُجدت
          if [ -s "$CA_CERTS" ]; then
            echo "--- Importing CA certificates ---"
            security import "$CA_CERTS" -k build.keychain -T /usr/bin/codesign -T /usr/bin/security 2>/dev/null || echo "⚠️  CA import skipped"
          fi
          
          # إعداد الوصول
          security set-key-partition-list -S apple-tool:,apple: -s -k "$KEYCHAIN_PASSWORD_CLEAN" build.keychain
          
          echo ""
          echo "--- Code signing identities ---"
          security find-identity -v -p codesigning build.keychain
          
          # معالجة provisioning profile
          echo ""
          echo "--- Installing provisioning profile ---"
          PROFILE_PLIST="$RUNNER_TEMP/profile.plist"
          security cms -D -i "$PROFILE" > "$PROFILE_PLIST"
          PROFILE_UUID=$(/usr/libexec/PlistBuddy -c 'Print :UUID' "$PROFILE_PLIST")
          PROFILE_NAME=$(/usr/libexec/PlistBuddy -c 'Print :Name' "$PROFILE_PLIST")
          TEAM_ID=$(/usr/libexec/PlistBuddy -c 'Print :TeamIdentifier:0' "$PROFILE_PLIST")
          
          # اكتشاف نوع provisioning profile تلقائياً
          echo "--- Detecting provisioning profile type ---"
          
          HAS_DEVICES="false"
          GET_TASK_ALLOW="unknown"
          
          if /usr/libexec/PlistBuddy -c 'Print :ProvisionedDevices' "$PROFILE_PLIST" &>/dev/null; then
            HAS_DEVICES="true"
            DEVICE_COUNT=$(/usr/libexec/PlistBuddy -c 'Print :ProvisionedDevices' "$PROFILE_PLIST" 2>/dev/null | grep -c "Dict" || echo "0")
            echo "ProvisionedDevices: YES (count: $DEVICE_COUNT)"
          else
            echo "ProvisionedDevices: NO (App Store profile)"
          fi
          
          GET_TASK_ALLOW_VALUE=$(/usr/libexec/PlistBuddy -c 'Print :Entitlements:get-task-allow' "$PROFILE_PLIST" 2>/dev/null || echo "not_found")
          echo "get-task-allow: $GET_TASK_ALLOW_VALUE"
          
          if [ "$HAS_DEVICES" = "true" ]; then
            if [ "$GET_TASK_ALLOW_VALUE" = "true" ]; then
              EXPORT_METHOD="development"
              echo "✅ Detected: Development provisioning profile"
            else
              EXPORT_METHOD="ad-hoc"
              echo "✅ Detected: Ad-Hoc provisioning profile"
            fi
          else
            EXPORT_METHOD="app-store"
            echo "✅ Detected: App Store provisioning profile"
          fi
          
          echo "Profile Name: $PROFILE_NAME"
          echo "Profile UUID: $PROFILE_UUID"
          echo "Team ID: $TEAM_ID"
          echo "Export Method: $EXPORT_METHOD"
          
          echo "PROFILE_NAME=$PROFILE_NAME" >> "$GITHUB_ENV"
          echo "PROFILE_UUID=$PROFILE_UUID" >> "$GITHUB_ENV"
          echo "TEAM_ID=$TEAM_ID" >> "$GITHUB_ENV"
          echo "EXPORT_METHOD=$EXPORT_METHOD" >> "$GITHUB_ENV"
          
          mkdir -p "$HOME/Library/MobileDevice/Provisioning Profiles"
          cp "$PROFILE" "$HOME/Library/MobileDevice/Provisioning Profiles/$PROFILE_UUID.mobileprovision"
          
          echo ""
          echo "✅ iOS code signing setup completed successfully"

      - name: Get Bundle ID for ${{ matrix.app }}
        if: ${{ github.event_name != 'pull_request' }}
        id: bundle-id
        working-directory: ./${{ matrix.app }}
        run: |
          # استخراج Bundle ID من ملف project.pbxproj أو Info.plist
          BUNDLE_ID=$(grep -m1 "PRODUCT_BUNDLE_IDENTIFIER" ios/Runner.xcodeproj/project.pbxproj | head -1 | sed 's/.*= *\(.*\);/\1/' | tr -d ' "')
          if [ -z "$BUNDLE_ID" ]; then
            # fallback: استخدام اسم التطبيق
            case "${{ matrix.app }}" in
              "hggzk_app") BUNDLE_ID="com.hggzk.app" ;;
              "hggzkportal_app") BUNDLE_ID="com.hggzk.portal" ;;
              "rezmate_app") BUNDLE_ID="com.rezmate.app" ;;
              "rezmateportal_app") BUNDLE_ID="com.rezmate.portal" ;;
              *) BUNDLE_ID="com.example.${{ matrix.app }}" ;;
            esac
          fi
          echo "BUNDLE_ID=$BUNDLE_ID" >> "$GITHUB_ENV"
          echo "Bundle ID: $BUNDLE_ID"

      - name: Build IPA (Automatic Signing with API Key)
        if: ${{ github.event_name != 'pull_request' }}
        env:
          API_KEY_ID: ${{ secrets.APPSTORE_API_KEY_ID }}
          API_ISSUER_ID: ${{ secrets.APPSTORE_API_ISSUER_ID }}
        working-directory: ./${{ matrix.app }}
        run: |
          set -euo pipefail
          
          echo "==================================="
          echo "🚀 Building IPA with Automatic Signing"
          echo "==================================="
          echo "App: ${{ matrix.app }}"
          echo "Bundle ID: $BUNDLE_ID"
          echo "Export Method: development (ad-hoc profile detected)"
          echo "Signing: Automatic (using App Store Connect API)"
          echo "Installable on: All registered devices in provisioning profile"
          echo ""
          
          # تحديد طريقة التوقيع
          USE_API_KEY=false
          if [ -n "${API_KEY_PATH:-}" ] && [ -f "${API_KEY_PATH}" ]; then
            USE_API_KEY=true
            echo "✅ App Store Connect API Key found - Using Automatic Signing"
          else
            echo "⚠️  App Store Connect API Key not found - Using Manual Signing"
          fi
          
          echo ""
          echo "--- Building Archive ---"
          
          if [ "$USE_API_KEY" = true ]; then
            # Build with automatic signing + API Key
            xcodebuild -workspace ios/Runner.xcworkspace \
              -scheme Runner \
              -configuration Release \
              -sdk iphoneos \
              -destination 'generic/platform=iOS' \
              -archivePath build/Runner.xcarchive \
              -authenticationKeyPath "${API_KEY_PATH}" \
              -authenticationKeyID "$API_KEY_ID" \
              -authenticationKeyIssuerID "$API_ISSUER_ID" \
              CODE_SIGN_STYLE=Automatic \
              -allowProvisioningUpdates \
              archive
          else
            # Fallback to manual signing
            # استخراج معلومات من provisioning profile
            PROFILE="../cer_ios/Provision_Profile_2.mobileprovision"
            PROFILE_PLIST="$RUNNER_TEMP/profile.plist"
            security cms -D -i "$PROFILE" > "$PROFILE_PLIST"
            
            TEAM_ID=$(/usr/libexec/PlistBuddy -c 'Print :TeamIdentifier:0' "$PROFILE_PLIST")
            PROFILE_NAME=$(/usr/libexec/PlistBuddy -c 'Print :Name' "$PROFILE_PLIST")
            PROFILE_UUID=$(/usr/libexec/PlistBuddy -c 'Print :UUID' "$PROFILE_PLIST")
            
            echo "Team ID: $TEAM_ID"
            echo "Profile Name: $PROFILE_NAME"
            
            xcodebuild -workspace ios/Runner.xcworkspace \
              -scheme Runner \
              -configuration Release \
              -sdk iphoneos \
              -destination 'generic/platform=iOS' \
              -archivePath build/Runner.xcarchive \
              DEVELOPMENT_TEAM="$TEAM_ID" \
              CODE_SIGN_STYLE=Manual \
              PROVISIONING_PROFILE_SPECIFIER="$PROFILE_NAME" \
              archive
          fi
          
          echo ""
          echo "✅ Archive created successfully"
          
          # معلومات عن الـ archive
          if [ -d "build/Runner.xcarchive/Products/Applications/Runner.app" ]; then
            echo ""
            echo "--- App Info ---"
            codesign -dv "build/Runner.xcarchive/Products/Applications/Runner.app" 2>&1 | grep -E "Authority|TeamIdentifier|Identifier" || true
          fi
          
          echo ""
          echo "--- Exporting IPA ---"
          
          # إنشاء ExportOptions.plist
          EXPORT_OPTIONS_PLIST="$RUNNER_TEMP/ExportOptions.plist"
          
          # تحديد export method - استخدام development دائماً
          # حتى مع Ad-Hoc profile (Xcode workaround)
          EXPORT_METHOD="development"
          
          echo "Export Method: $EXPORT_METHOD (forced for Xcode compatibility)"
          echo ""
          
          # ULTIMATE FIX for Xcode:
          # المشكلة: Xcode يتوقع {} لكن يجد string
          # الحل: استخدام defaults write (NSUserDefaults) لإنشاء plist صحيح
          
          EXPORT_OPTIONS_PLIST="$RUNNER_TEMP/ExportOptions.plist"
          
          # حذف الملف إن وُجد
          rm -f "$EXPORT_OPTIONS_PLIST"
          
          # تحديد signing style بناءً على طريقة الـ archive
          if [ "$USE_API_KEY" = true ]; then
            SIGNING_STYLE="automatic"
            echo "Signing Style: automatic (matches archive)"
          else
            SIGNING_STYLE="manual"
            echo "Signing Style: manual (matches archive)"
          fi
          
          # إنشاء plist باستخدام defaults (أكثر موثوقية من PlistBuddy)
          defaults write "$RUNNER_TEMP/ExportOptions" teamID "$TEAM_ID"
          defaults write "$RUNNER_TEMP/ExportOptions" method "$EXPORT_METHOD"
          defaults write "$RUNNER_TEMP/ExportOptions" signingStyle "$SIGNING_STYLE"
          defaults write "$RUNNER_TEMP/ExportOptions" compileBitcode -bool false
          defaults write "$RUNNER_TEMP/ExportOptions" uploadSymbols -bool false
          defaults write "$RUNNER_TEMP/ExportOptions" stripSwiftSymbols -bool true
          
          # إضافة provisioningProfiles فقط للتوقيع اليدوي
          if [ "$SIGNING_STYLE" = "manual" ]; then
            defaults write "$RUNNER_TEMP/ExportOptions" provisioningProfiles -dict-add "$BUNDLE_ID" "$PROFILE_UUID"
          fi
          
          echo "--- ExportOptions.plist (defaults method) ---"
          plutil -p "$EXPORT_OPTIONS_PLIST"
          echo ""
          
          # Export IPA
          if [ "$USE_API_KEY" = true ]; then
            xcodebuild -exportArchive \
              -archivePath build/Runner.xcarchive \
              -exportOptionsPlist "$EXPORT_OPTIONS_PLIST" \
              -exportPath build/ipa \
              -authenticationKeyPath "${API_KEY_PATH}" \
              -authenticationKeyID "$API_KEY_ID" \
              -authenticationKeyIssuerID "$API_ISSUER_ID" \
              -allowProvisioningUpdates
          else
            xcodebuild -exportArchive \
              -archivePath build/Runner.xcarchive \
              -exportOptionsPlist "$EXPORT_OPTIONS_PLIST" \
              -exportPath build/ipa \
              -allowProvisioningUpdates
          fi
          
          echo ""
          echo "==================================="
          echo "✅ IPA Export Successful!"
          echo "==================================="
          echo "📦 Export Method: development"
          if [ "$USE_API_KEY" = true ]; then
            echo "🔐 Signing: Automatic (API Key)"
          else
            echo "🔐 Signing: Manual"
          fi
          echo "📱 Installable on all registered devices"
          echo ""
          
          ls -lh build/ipa/*.ipa || ls -la build/ipa

      - name: Rename IPA with app name
        if: ${{ github.event_name != 'pull_request' }}
        working-directory: ./${{ matrix.app }}
        run: |
          VERSION=$(grep 'version:' pubspec.yaml | cut -d ' ' -f 2 | cut -d '+' -f 1)
          APP_NAME="${{ matrix.app }}"
          IPA_NAME="${APP_NAME}-v${VERSION}-iOS.ipa"
          if ls build/ipa/*.ipa 1> /dev/null 2>&1; then
            mv build/ipa/*.ipa "build/ipa/${IPA_NAME}"
            echo "IPA_NAME=${IPA_NAME}" >> $GITHUB_ENV
            echo "VERSION=${VERSION}" >> $GITHUB_ENV
          fi

      - name: Upload IPA artifact
        if: ${{ github.event_name != 'pull_request' }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.app }}-ios-ipa
          path: ./${{ matrix.app }}/build/ipa/*.ipa

  build-android:
    name: Build ${{ matrix.app }} for Android
    needs: setup
    if: ${{ github.event.inputs.platform != 'ios' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        app: ${{ fromJson(needs.setup.outputs.matrix) }}
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          cache: 'gradle'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: SSH debug (tmate)
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.ssh }}
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: true

      - name: Flutter version
        run: flutter --version

      - name: Flutter clean
        working-directory: ./${{ matrix.app }}
        run: flutter clean

      - name: Flutter pub get
        working-directory: ./${{ matrix.app }}
        run: flutter pub get

      - name: Build APK (Debug) for PR
        if: ${{ github.event_name == 'pull_request' }}
        working-directory: ./${{ matrix.app }}
        run: flutter build apk --debug

      - name: Setup Android Keystore
        if: ${{ github.event_name != 'pull_request' }}
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          set -euo pipefail
          
          if [ -n "${ANDROID_KEYSTORE_BASE64:-}" ]; then
            echo "--- Setting up Android Keystore ---"
            
            # فك تشفير الـ keystore
            KEYSTORE_PATH="$RUNNER_TEMP/upload-keystore.jks"
            echo "$ANDROID_KEYSTORE_BASE64" | base64 -d > "$KEYSTORE_PATH"
            
            # إنشاء ملف key.properties
            KEY_PROPERTIES="./${{ matrix.app }}/android/key.properties"
            cat > "$KEY_PROPERTIES" << EOF
          storePassword=$ANDROID_KEYSTORE_PASSWORD
          keyPassword=$ANDROID_KEY_PASSWORD
          keyAlias=$ANDROID_KEY_ALIAS
          storeFile=$KEYSTORE_PATH
          EOF
            
            echo "✅ Android Keystore configured"
            echo "KEYSTORE_PATH=$KEYSTORE_PATH" >> "$GITHUB_ENV"
          else
            echo "⚠️  Android Keystore not provided - will build unsigned APK"
          fi

      - name: Build APK (Release)
        if: ${{ github.event_name != 'pull_request' }}
        working-directory: ./${{ matrix.app }}
        run: |
          if [ -n "${KEYSTORE_PATH:-}" ]; then
            flutter build apk --release
          else
            flutter build apk --release
          fi

      - name: Build App Bundle (AAB)
        if: ${{ github.event_name != 'pull_request' }}
        working-directory: ./${{ matrix.app }}
        run: |
          if [ -n "${KEYSTORE_PATH:-}" ]; then
            flutter build appbundle --release
          else
            flutter build appbundle --release
          fi

      - name: Rename APK with app name
        if: ${{ github.event_name != 'pull_request' }}
        working-directory: ./${{ matrix.app }}
        run: |
          VERSION=$(grep 'version:' pubspec.yaml | cut -d ' ' -f 2 | cut -d '+' -f 1)
          APP_NAME="${{ matrix.app }}"
          APK_NAME="${APP_NAME}-v${VERSION}-Android.apk"
          AAB_NAME="${APP_NAME}-v${VERSION}-Android.aab"
          
          if [ -f "build/app/outputs/flutter-apk/app-release.apk" ]; then
            mv "build/app/outputs/flutter-apk/app-release.apk" "build/app/outputs/flutter-apk/${APK_NAME}"
          fi
          
          if [ -f "build/app/outputs/bundle/release/app-release.aab" ]; then
            mv "build/app/outputs/bundle/release/app-release.aab" "build/app/outputs/bundle/release/${AAB_NAME}"
          fi
          
          echo "APK_NAME=${APK_NAME}" >> $GITHUB_ENV
          echo "AAB_NAME=${AAB_NAME}" >> $GITHUB_ENV
          echo "VERSION=${VERSION}" >> $GITHUB_ENV

      - name: Upload APK artifact
        if: ${{ github.event_name != 'pull_request' }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.app }}-android-apk
          path: ./${{ matrix.app }}/build/app/outputs/flutter-apk/*.apk

      - name: Upload AAB artifact
        if: ${{ github.event_name != 'pull_request' }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.app }}-android-aab
          path: ./${{ matrix.app }}/build/app/outputs/bundle/release/*.aab

  notify-completion:
    name: Notify Build Completion
    needs: [setup, build-ios, build-android]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Build Status
        run: |
          IOS_STATUS="${{ needs.build-ios.result }}"
          ANDROID_STATUS="${{ needs.build-android.result }}"
          
          echo "iOS Build Status: $IOS_STATUS"
          echo "Android Build Status: $ANDROID_STATUS"
          
          if [ "$IOS_STATUS" == "success" ] || [ "$IOS_STATUS" == "skipped" ]; then
            echo "✅ iOS builds completed!"
          else
            echo "❌ iOS builds failed!"
          fi
          
          if [ "$ANDROID_STATUS" == "success" ] || [ "$ANDROID_STATUS" == "skipped" ]; then
            echo "✅ Android builds completed!"
          else
            echo "❌ Android builds failed!"
          fi

      - name: Summary
        run: |
          echo "## 📱 Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**iOS Status:** ${{ needs.build-ios.result }}" >> $GITHUB_STEP_SUMMARY
          echo "**Android Status:** ${{ needs.build-android.result }}" >> $GITHUB_STEP_SUMMARY
          echo "**Build Number:** ${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Built Apps" >> $GITHUB_STEP_SUMMARY
          echo "${{ needs.setup.outputs.matrix }}" >> $GITHUB_STEP_SUMMARY