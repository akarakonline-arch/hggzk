# تقرير إصلاح بطاقات اختيار العقار والوحدة

## 📋 المشكلة الأصلية

عند فتح صفحة الجدول اليومي (`DailySchedulePage`):
1. ❌ **لا تظهر بطاقات اختيار العقار والوحدة** - الصفحة كانت فارغة تماماً
2. ❌ **البطاقات كانت تستخدم بيانات وهمية** - عند النقر على اختيار العقار/الوحدة كانت تظهر قوائم ثابتة

---

## ✅ الحل المطبق

### 1. إصلاح مشكلة عدم ظهور البطاقات

**المشكلة:**
- البطاقات كانت تظهر فقط عندما تكون الحالة `DailyScheduleLoaded`
- عند فتح الصفحة أول مرة، الحالة `DailyScheduleInitial` تعرض فقط رسالة نصية

**الحل:**
تعديل `_buildInitialState()` لتعرض بطاقات اختيار العقار والوحدة:

```dart
Widget _buildInitialState() {
  final isDesktop = size.width > AppDimensions.desktopBreakpoint;

  return Column(
    children: [
      if (isDesktop) ...[
        Row(
          children: [
            SizedBox(
              width: 320,
              child: Column(
                children: [
                  // ✅ بطاقة اختيار العقار
                  RealPropertySelectorCard(...),
                  
                  // ✅ بطاقة اختيار الوحدة
                  RealUnitSelectorCard(...),
                ],
              ),
            ),
            // رسالة إرشادية
            Expanded(child: Center(...)),
          ],
        ),
      ] else ...[
        // نفس الشيء للموبايل
      ],
    ],
  );
}
```

---

### 2. استبدال البطاقات الوهمية ببطاقات حقيقية

**تم إنشاء بطاقتين جديدتين:**

#### أ. `RealPropertySelectorCard` ✅
📁 `lib/features/admin_daily_schedule/presentation/widgets/real_property_selector_card.dart`

**الميزات:**
- ✅ تستخدم `context.push('/helpers/search/properties')` للانتقال لصفحة البحث الحقيقية
- ✅ تستقبل بيانات حقيقية من API
- ✅ نفس الطريقة المستخدمة في فيتشر الوحدات

**الكود:**
```dart
void _openPropertySearch(BuildContext context) {
  HapticFeedback.lightImpact();
  context.push(
    '/helpers/search/properties',
    extra: {
      'allowMultiSelect': false,
      'onPropertySelected': (Property property) {
        onPropertySelected(property.id, property.name ?? '');
      },
    },
  );
}
```

#### ب. `RealUnitSelectorCard` ✅
📁 `lib/features/admin_daily_schedule/presentation/widgets/real_unit_selector_card.dart`

**الميزات:**
- ✅ تستخدم `context.push('/helpers/search/units')` للانتقال لصفحة البحث الحقيقية
- ✅ تمرر `propertyId` للتصفية
- ✅ تستقبل بيانات حقيقية من API
- ✅ نفس الطريقة المستخدمة في فيتشر الخدمات

**الكود:**
```dart
void _openUnitSearch(BuildContext context) {
  HapticFeedback.lightImpact();
  context.push(
    '/helpers/search/units',
    extra: {
      'propertyId': widget.selectedPropertyId,
      'allowMultiSelect': false,
      'onUnitSelected': (Unit unit) {
        widget.onUnitSelected(unit.id, unit.name);
      },
    },
  );
}
```

---

### 3. التحديثات في `daily_schedule_page.dart`

**التغييرات:**

1. **إضافة Imports:**
```dart
import 'package:go_router/go_router.dart';
import '../../../admin_properties/domain/entities/property.dart';
import '../../../admin_units/domain/entities/unit.dart';
```

2. **استبدال جميع البطاقات:**
```bash
SimplePropertySelectorCard → RealPropertySelectorCard
SimpleUnitSelectorCard → RealUnitSelectorCard
```

3. **في 4 أماكن:**
   - ✅ `_buildInitialState()` - Desktop
   - ✅ `_buildInitialState()` - Mobile
   - ✅ `_buildDesktopLayout()`
   - ✅ `_buildMobileLayout()`

---

## 📊 النتيجة النهائية

### ✅ **قبل الإصلاح:**
```
❌ صفحة فارغة تماماً
❌ لا يمكن اختيار عقار أو وحدة
❌ بيانات وهمية عند المحاولة
```

### ✅ **بعد الإصلاح:**
```
✅ تظهر بطاقات اختيار العقار والوحدة مباشرة
✅ النقر على "اختر العقار" ينقلك لصفحة بحث حقيقية
✅ النقر على "اختر الوحدة" ينقلك لصفحة بحث حقيقية
✅ البيانات تأتي من API
✅ نفس تجربة المستخدم في فيتشر الوحدات والخدمات
```

---

## 📁 الملفات المُعدلة والجديدة

### ملفات جديدة (2):
1. ✅ `real_property_selector_card.dart` - بطاقة اختيار العقار الحقيقية
2. ✅ `real_unit_selector_card.dart` - بطاقة اختيار الوحدة الحقيقية

### ملفات مُعدلة (2):
1. ✅ `daily_schedule_page.dart` - تعديل `_buildInitialState()` واستبدال البطاقات
2. ✅ `widgets_barrel.dart` - إضافة export للبطاقات الجديدة

---

## 🎯 كيفية الاستخدام

### 1. افتح صفحة الجدول اليومي
```dart
Navigator.push(context, MaterialPageRoute(
  builder: (_) => DailySchedulePage(),
));
```

### 2. ستظهر لك بطاقتان:
- **اختر العقار** 🏢
- **اختر الوحدة** 🚪 (مقفلة حتى تختار عقار)

### 3. اختر العقار:
- اضغط على بطاقة "اختر العقار"
- ستفتح صفحة بحث العقارات
- ابحث واختر العقار المطلوب
- سيتم حفظ العقار المختار

### 4. اختر الوحدة:
- بعد اختيار العقار، بطاقة الوحدة ستصبح نشطة
- اضغط على بطاقة "اختر الوحدة"
- ستفتح صفحة بحث الوحدات (مصفاة حسب العقار)
- اختر الوحدة المطلوبة

### 5. سيتم تحميل الجدول تلقائياً! 🎉
```dart
context.read<DailyScheduleBloc>().add(
  LoadMonthlyScheduleEvent(
    unitId: selectedUnitId,
    year: currentYear,
    month: currentMonth,
  ),
);
```

---

## ✅ التحقق من عدم وجود أخطاء

```bash
flutter analyze lib/features/admin_daily_schedule/
# النتيجة: 0 errors ✅
```

---

## 🎨 التحسينات الإضافية

1. ✅ **أنيميشن توهج** في بطاقة الوحدة
2. ✅ **تعطيل بطاقة الوحدة** حتى يتم اختيار العقار
3. ✅ **Haptic Feedback** عند النقر
4. ✅ **تصميم موحد** مع باقي الفيتشرات
5. ✅ **Responsive Design** - Desktop و Mobile

---

**تاريخ الإصلاح:** 2025-11-19  
**الحالة:** ✅ مكتمل ويعمل بكفاءة
